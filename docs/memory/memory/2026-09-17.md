# 2026-09-17 Operational Memory

<!-- FANTASY_MEMORY_PUSH_V1_NATIVE_STDERR_FAILURE:BEGIN -->
## Memory checkpoint push v1 — failed before commit/push

The first `fantasy_memory_checkpoint_push_v1` Windows run reached the isolated
GitHub clone step and stopped with PowerShell `NativeCommandError` on Git's normal
`Cloning into '...'` progress message.

Classification: `FAILED BEFORE COMMIT/PUSH`.

Measured facts:
- the local project Git root resolved successfully;
- no football/model source was modified by the push tool;
- no staging commit had been created;
- no `git push` had executed;
- a read-only GitHub check afterward confirmed `main` remained at
  `a97e0066d89f921c65811aca5b983d216d481b2e`.

Root cause:
Windows PowerShell 5.1 can promote native-process stderr records into terminating
`NativeCommandError` under `$ErrorActionPreference = 'Stop'`, even when the
native program is only writing normal progress output.

v2 correction:
the Git wrapper temporarily uses non-terminating PowerShell error handling while
Git executes, captures combined output, restores the prior preference, and judges
success/failure from `$LASTEXITCODE`.

This is a tooling/procedure correction only. Football physics/model code is
unchanged.
<!-- FANTASY_MEMORY_PUSH_V1_NATIVE_STDERR_FAILURE:END -->

<!-- FANTASY_MANIFEST_REPAIR_V1_PARSER_FAILURE:BEGIN -->
## Manifest repair v1 — PowerShell parser failure before modification

The first `fantasy_memory_manifest_repair_v1` local run did not execute because
Windows PowerShell rejected two interpolated exception strings at parse time:

- `Byte-count mismatch for $rel: ...`
- `SHA-256 mismatch for $rel: ...`

Classification: `FAILED BEFORE MODIFICATION`.

Because parsing failed before script execution:
- no local durable-memory file was modified by v1;
- no backup/write phase started;
- no staging clone was created;
- no commit was created;
- no push ran;
- a subsequent read-only GitHub check confirmed `main` remained at
  `28485d27bd436f6f158da2a4d34671497238559a`.

Root cause:
in a double-quoted PowerShell string, an unbraced variable immediately followed
by `:` can be parsed as a scoped/drive-style variable reference. `$rel:` is
therefore not a safe interpolation form.

v2 correction:
the affected messages use the `-f` format operator rather than adjacent
`$variable:` interpolation. Package validation also scans the script for this
parser-risk pattern before delivery.

This is a tooling-only correction. Football/model/application source remains
unchanged.
<!-- FANTASY_MANIFEST_REPAIR_V1_PARSER_FAILURE:END -->

<!-- FANTASY_2026_09_17_V10A_ROADMAP_START:BEGIN -->
## Phase 0 v1.4 interpretation and v1.0A roadmap start

Phase 0 reconciliation probe v1.4 completed successfully at approximately 02:08 local time.

Measured facts retained:
- baseline `0.35-fixed1 @ c85434a...` retrieved and VERSION verified;
- local v0.36 directory and ZIP compared against the public baseline;
- v0.36 ZIP: 23 changed, 3 added, 7 removed public-safe files;
- 330 sanitized provenance text hits collected;
- non-memory Git porcelain unchanged;
- football/model source modification: NO;
- durable memory updated by the probe.

The final classifier `FIXED1_NAMED_ARTIFACT_FOUND_NEEDS_CONTENT_VALIDATION` is superseded as representation-incomplete. Its name-based fixed1 test could be satisfied by the known `fantasy_season_v0_35_fixed1` baseline artifacts. Raw measurements remain valid and have higher authority than the classifier.

User requested a roadmap change making diagnostics/logging modular and adaptable, including explicit GUI diagnostics. Decision D-008 and `architecture/DIAGNOSTICS_OBSERVABILITY.md` establish v1.0A as the shared observability substrate.

This roadmap/memory update starts v1.0A with a source-safe static diagnostic surface audit. It does not alter football physics or application source.
<!-- FANTASY_2026_09_17_V10A_ROADMAP_START:END -->

<!-- FANTASY_PRIVYHUB_STYLE_ALIGNMENT_2026_09_17:BEGIN -->
## PrivyHub-style workflow alignment

The user clarified the intended repository workflow:

- memory updates are not merely prepared locally; they are pushed as part of the checkpoint;
- the push mechanism is the self-contained ZIP/PowerShell workflow used in the PrivyHub-style `onn-stream-test` process;
- diagnostic/probe/audit/observability tooling has the same standing development/checkpoint permission as durable memory;
- football/model/application/business-logic code remains gated behind explicit user authorization at the end of a checkpoint.

A direct GitHub connector write path was attempted while correcting this rule and was rejected by the user as the wrong project workflow. That path is superseded and must not be used for project checkpoint writes.

Future memory/diagnostic changes should therefore be:
`ZIP -> local apply/validate -> exact allowlist -> push -> remote verification`.

No football/model/application source change is part of this alignment.
<!-- FANTASY_PRIVYHUB_STYLE_ALIGNMENT_2026_09_17:END -->

<!-- FANTASY_MANIFEST_GIT_BLOB_SEMANTICS_2026_09_17:BEGIN -->
## Manifest worktree-vs-Git-blob representation defect

After the PrivyHub-style workflow alignment checkpoint pushed successfully,
a read-only repository audit confirmed the intended workflow and exact memory-only
scope.

The audit also found a narrower manifest defect: the registry hashes/byte counts
were generated from Windows worktree files before Git's LF normalization, so they
do not equal the durable committed blob representation.

Examples at checkpoint `4b979b4c...`:
- `USER.md`: registry 3316 bytes vs committed blob 3268 bytes;
- `AGENTS.md`: registry 7396 bytes vs committed blob 7221 bytes.

This does not invalidate the memory content or checkpoint scope. It means the
manifest cannot yet be used as an exact verifier of GitHub checkpoint bytes.

Successor repair: generate/validate the registry from staged Git index bytes and
revalidate from committed `HEAD` bytes. This is diagnostic/memory tooling only.
<!-- FANTASY_MANIFEST_GIT_BLOB_SEMANTICS_2026_09_17:END -->

<!-- FANTASY_MEMORY_INFRA_READY_VALIDATION_2026_09_17:BEGIN -->
## Memory / diagnostic infrastructure readiness validation

Read-only GitHub audit after the manifest Git-blob repair verified:

- `main = 243e4ee4906f42582a5603170e7d7c73095c9dac`;
- commit message: `Repair memory manifest Git-blob semantics`;
- the repair is one fast-forward commit from the prior PrivyHub-alignment
  checkpoint;
- every changed file is under `docs/memory/**`;
- manifest schema `2`;
- representation `git_index_blob_bytes`;
- representative manifest byte counts now agree with committed Git tree values
  (`USER.md = 3268`, `AGENTS.md = 7221`);
- no football/model/application source changed.

I-005 is therefore closed as `RESOLVED / RUNTIME-VALIDATED`.

The memory/diagnostic checkpoint infrastructure is ready for normal project use.
<!-- FANTASY_MEMORY_INFRA_READY_VALIDATION_2026_09_17:END -->

<!-- FANTASY_I001_V2_2026_09_17:BEGIN -->
## I-001 evidence review v2

Timestamp: `2026-09-17T03:06:26.836159-04:00`

- predecessor v1: `FAILED BEFORE MODIFICATION` because the old v1.4 result ZIP
  was absent;
- successor v2 removed that brittle dependency and rebuilt evidence from the
  exact local v0.36 artifact plus committed v0.35-fixed1 baseline;
- result: `V036_EXACT_RELEASE_PRESENT_FIXED1_NOT_ESTABLISHED`;
- authority: `V036_LATEST_EXACT_ARTIFACT_FIXED1_UNPROVEN`;
- football/model/application source changed: NO.

Failure history is preserved in I-001.
<!-- FANTASY_I001_V2_2026_09_17:END -->

<!-- FANTASY_DIAGNOSTIC_QA_FAILURE_20260917:BEGIN -->
## GUI-lineage diagnostic failed; QA gate strengthened

`fantasy_phase0_gui_lineage_isolation_20260917_v1` failed at runtime with:

`NameError: name 'sys' is not defined`

The failing helper was `run_release_checks()`.

Classification:
`FAILED DURING DIAGNOSTIC EXECUTION / BEFORE MEMORY COMMIT-PUSH`.

No football/model/application source was modified.

Full audit identified four validation/design issues:
1. missing `sys` import;
2. runtime-critical release-check helper absent from self-test coverage;
3. staging cleanup not guaranteed by `finally`;
4. GUI-only classification and idempotent/local-sync behavior needed tighter
   boundaries.

A diagnostic-tool QA protocol is now required before the successor is delivered.
<!-- FANTASY_DIAGNOSTIC_QA_FAILURE_20260917:END -->

<!-- FANTASY_GUI_LINEAGE_V2_FAILURE_20260917:BEGIN -->
## GUI-lineage v2 package invalidated

At approximately 03:31–03:32 local time,
`fantasy_phase0_gui_lineage_isolation_20260917_v2.zip` ran far enough to produce
useful measurements, then failed before commit/push.

**Package status:** `INVALID / NOT PACKAGE-VALIDATED / SUPERSEDED`.

The earlier assistant claim that the package had passed complete package
validation was incorrect.

Valid measurements:
- v0.36 ZIP SHA:
  `598c518ad30e2f1c65c452bafca111808f5456eeb818bf04c43994ceb269987c`
- source delta: `0 changed / 0 added / 0 removed`
- classification: `NO_LOCAL_SOURCE_SUCCESSOR_DELTA`
- compileall exit: `0`
- pytest exit: `1` (`UNRESOLVED`)
- evidence SHA:
  `e64094a5b5da9205f65b18176c641b08dec9bf4116dc128e425755fbe77587c0`

Failure:
`git diff --cached --check` rejected trailing whitespace in generated
`docs/memory/evidence/I001_GUI_LINEAGE_ISOLATION_V2_2026-09-17.md`.

No football/model/application source changed.

The QA protocol is extended to validate rendered memory/evidence cleanliness
before delivery.
<!-- FANTASY_GUI_LINEAGE_V2_FAILURE_20260917:END -->

<!-- FANTASY_V036_PYTEST_DIAGNOSIS_HISTORY_20260917:BEGIN -->
## Exact v0.36 pytest diagnosis

Timestamp: `2026-09-17T03:50:59.301342-04:00`

- diagnosis: `V036_SPECIFIC_DETERMINISTIC_TEST_REGRESSION`;
- v0.36 pytest exit: `1`;
- v0.36 parsed failures: `2`;
- baseline pytest exit: `0`;
- baseline parsed failures: `0`;
- evidence SHA-256:
  `db44cb6ec8929f1818eb0aa786ce77a53d6af8943067171011ab69708ac9d3b7`;
- production source changed: NO.
<!-- FANTASY_V036_PYTEST_DIAGNOSIS_HISTORY_20260917:END -->

<!-- FANTASY_V036_FIXTURE_RESTORATION_HISTORY_20260917:BEGIN -->
## v0.36 fixture-restoration probe

Timestamp: `2026-09-17T04:04:48.120222-04:00`

- classification: `V036_RELEASE_PACKAGING_FIXTURE_REGRESSION_CONFIRMED`;
- before targeted exits:
  `[1, 1]`;
- after targeted exits:
  `[0, 0]`;
- full suite after restoration: `0`;
- evidence SHA-256:
  `823fd30bf500dd4835bbc4cfa942774783a6958bf8f5c8a7c2da2b2d769e02f9`;
- production source changed: NO.
<!-- FANTASY_V036_FIXTURE_RESTORATION_HISTORY_20260917:END -->
