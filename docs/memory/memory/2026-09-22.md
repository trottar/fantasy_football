# 2026-09-22

## M7 durability closure

Read-only remote verification established that `main` resolved to the exact M7
fresh-session integration checkpoint and contained the evidence plus continuity
state required by the M7 completion condition.

Classification:

`M0-M7 MEMORY-SYSTEM REFINEMENT = COMPLETE / DURABLE`

Canonical evidence:
`../evidence/MEMORY_M7_DURABILITY_CLOSURE_2026-09-22.md`.

This transition changed no football/model/runtime behavior.

## Week 3 week-open prospective capture

The first future hard Week 3 capture gate was executed against commissioned
`v0.36-repack1`.

Classification:

`PROSPECTIVE_WEEK_OPEN_CAPTURE_VALID`

The snapshot was frozen at `2026-09-22T14:10:51.714616Z` and the prediction
capture at `2026-09-22T14:10:53.254794Z`, both before the configured Week 3 first
game deadline `2026-09-25T00:15:00Z`.

Validation returned capture integrity PASS, pre-data firewall PASS, all configured
source groups OK, and no degraded source groups. The sanitized measurement
contained 32 matchup player records, 174 all-league player predictions, 64
specialists (32 DST / 32 K), 12 behavior teams, and 846 market players.

Authenticated/raw evidence remains local. The diagnostic package performed no
repository write, no runtime-source write, and did not enable persistent
observability.

Canonical sanitized evidence:
`../evidence/WEEK3_WEEK_OPEN_PROSPECTIVE_CAPTURE_2026-09-22.md`.

## Frontier transition

With the irreversible Week 3 week-open state secured, the active technical
frontier returns to the retained Phase 1B closure-instrumentation candidate.

Resume from the last validated Phase 1B preflight gate. Do not rerun already
passed targeted tests, paired non-interference/privacy probe, full pytest,
compileall, or diff/allowlist gates unless new evidence invalidates them.

Persistent runtime evidence remains disabled.
## Phase 1B retained-candidate recovery and reconstruction failure

Direct recovery work established that the previously retained Phase 1B
closure-shadow candidate byte identity is no longer available in surviving Git
worktrees, retained `.ffpkg` source carriers, or the remaining generic-delivery
staging root.

The old targeted/probe/full-validation measurements remain historical evidence
for the lost candidate only. They do not authorize reconstructed bytes.

A first fresh reconstruction package,
`phase1b_closure_shadow_fresh_candidate_20260922_v1`, verified the intended
remote-verified predecessor HEAD and source blob identities and created only its
owned isolated candidate. It then failed during source transformation because a
raw byte-marker helper reported two matches where one was expected.

Classification:

`FAILED IN OWNED ISOLATED CANDIDATE / SOURCE-TRANSFORM ASSUMPTION DEFECT`

No authoritative control-root source, commissioned runtime source, Git
index/history, or remote state changed. Targeted pytest and the paired Phase 1B
probe did not run.

The active Phase 1B state is therefore:

`RECOVERY / FRESH REBUILD REQUIRED / NOT YET PREFLIGHT VALIDATED`

Canonical evidence:
`../evidence/PHASE1B_RECOVERY_FAILURE_LINEAGE_2026-09-22.md`.
## Phase 1B fresh closure-shadow source validation

The fresh Phase 1B v2 candidate is now fully source validated.

Exact source inspection established that `src/closure.py` intentionally contains
two top-level `build_pregame_capture_from_context` definitions. The first is
aliased as `_build_pregame_capture_from_context_pre_v034`; only the final v0.34
public override is instrumented.

Fresh v2 uses exactly four technical paths and leaves the commissioned shared
`shadow_pilot.py` unchanged.

The first v2 validation run hit a validator-only defect: Git stderr containing a
benign CRLF warning was merged into stdout and parsed as an extra changed path.
The retained candidate bytes were not invalidated.

The v3 continuation made no source changes and passed:

- exact retained-byte/four-path identity;
- structural final-v0.34 boundary;
- targeted pytest: 33 passed in 1.94 s;
- paired output/exception/state/privacy/RNG/overhead probe.

The paired observer timing was 6,100 ns baseline versus 70,100 ns observed,
64,000 ns incremental. The 10.491803278688524 relative fraction is
non-authoritative because the baseline is below the configured 50,000,000 ns
relative floor; the 64,000 ns absolute increment passes the 2,000,000 ns budget.

The exact retained bytes then passed the full source gate:

- full pytest: 491 passed in 51.83 s;
- full compileall: PASS;
- `git diff --check`: PASS;
- post-validation exact-byte/four-path identity: PASS.

Classification:

`FRESH_PHASE1B_FULLY_SOURCE_VALIDATED`

The exact validated four-file byte identity is now applied to the control-root
checkpoint surface with durable memory. Repository staging/publication and
commissioned-runtime synchronization remain separate pending gates.

Canonical evidence:
`../evidence/PHASE1B_CLOSURE_SHADOW_SOURCE_VALIDATION_2026-09-22.md`.
