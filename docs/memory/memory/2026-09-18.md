# 2026-09-18 Operational Memory

## Durable-memory / handoff reconciliation

A read-only repository and project-context audit was performed before continuing
v1.0A work.

Reference GitHub state observed during the audit:
- `main = f22115e7c75357e4321912831a3f99d1ea3e5c3f`;
- commit: `Enable v1.0A GUI lifecycle shadow pilot`;
- parent: `eb7fc236a61f50459397cf3e2f58cd1cdbb1091b`.

The validated local tree remains authoritative between checkpoints.

## Audit findings

The audit found four operational/documentation issues:

1. bootstrap documents still described a three-file startup even though the
   project workflow requires substantial sessions to consult `AGENTS.md`,
   `CURRENT.md`, `MEMORY.md`, `handoffs/CURRENT_HANDOFF.md`, and `USER.md`;

2. repository memory already prohibited direct GitHub connector checkpoint
   writes, but the current handoff did not make the user/assistant actor
   separation sufficiently explicit;

3. the top-level roadmap summary still named the completed failure-bundle
   contract as the exact next slice, despite newer validated GUI-lifecycle state;

4. several philosophy/roadmap conclusions from the project discussion had not
   yet been fully promoted into curated durable memory.

Canonical audit:
`../evidence/MEMORY_WORKFLOW_AUDIT_2026-09-18.md`.

## Workflow incident classification

During the fresh-chat audit/reconciliation, a direct GitHub write route was
attempted instead of following the established package/local-validation
workflow.

Classification:

`PROCEDURAL WORKFLOW VIOLATION / NO VALID PROJECT CHECKPOINT`

No direct connector write is treated as project authority. The corrected durable
rule is that checkpoint evidence must cross the local validation boundary.

## User-confirmed checkpoint workflow

The user explicitly reconfirmed the project workflow:

1. assistant supplies the update/package;
2. user runs the `.ps1` locally;
3. user returns the complete output;
4. assistant verifies the result;
5. assistant sends separate commands for staging/commit/push;
6. user executes those commands;
7. remote state can then be checked read-only.

A normal delivered `.ps1` must not stage, commit, or push.

## Durable scientific promotion

The memory update also promotes these project-wide principles:

- observability is experimental measurement infrastructure, not merely logging;
- the MC is a microscope, not the theory;
- correct state/response/uncertainty precede sampling volume;
- constructive disagreement is required;
- historical data is contextual prior evidence because football is
  non-stationary;
- the physics analogy guides state, uncertainty, residual, perturbation, and
  coupled-channel structure but does not imply deterministic universal laws.


## Pre-delivery QA correction

The first candidate reconciliation payload updated the bootstrap contract in
memory documents but left `tools/check_memory_health.py` encoding the old
three-file startup order. Exact payload QA caught that mismatch before delivery:
strict memory health would have rejected the corrected `AGENTS.md`.

The successor payload updates the observational health tool to schema 2 and the
five-file bootstrap contract. This is tooling/memory maintenance only; no
football/model/application behavior changes.

## Current technical frontier preserved

No technical frontier change is introduced by this memory maintenance.

Current state remains:
- v0.36-repack1: `COMMISSIONED`;
- active phase: `v1.0A observability`;
- latest completed technical slice: GUI background-task/lifecycle shadow pilot;
- next technical slice: data-source season-sync shadow pilot;
- persistent sink: disabled;
- player/DST/K/market/closure instrumentation: separately gated.

Football/model/application source change:
`NONE`.

Runtime-tree change:
`NONE`.
