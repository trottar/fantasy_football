# Memory M3B Curated Durable-Memory Cleanup — 2026-09-21

## Classification

`M3B CURATED DURABLE MEMORY CLEANUP = CONTENT COMPLETE / NO FOOTBALL OR RUNTIME CHANGE`

M3B is the second half of M3. M3A reconciled procedure ownership; M3B restores
`MEMORY.md` to curated cross-phase knowledge rather than sequential active state,
procedure duplication, and tooling chronology.

## Problem

Before M3B, `MEMORY.md` mixed valid durable scientific knowledge with four kinds
of role drift identified by the M0 audit:

1. a detailed repository actor/procedure sequence already owned by
   `patches/PATCH_PROTOCOL.md`;
2. the current mandatory startup sequence already owned by bootstrap/maintenance
   policy;
3. a Phase 1A pre-commissioning candidate section immediately followed by its
   commissioned successor;
4. generic staging failure chronology that belongs in evidence/lessons rather
   than curated project knowledge.

That mixture made `MEMORY.md` partially chronological and duplicated facts whose
canonical owner already existed.

## Curated Ownership After M3B

`MEMORY.md` now keeps:

- project purpose and physics/state-response model;
- `P ⊕ D ⊕ K`;
- counterfactual/weekly-state and causal Data/MC rules;
- historical non-stationarity principle;
- manager-behavior separation;
- v0.X/v1.X scientific boundary;
- commissioned v0.36 baseline conclusions with canonical pointers;
- stable observability architecture;
- commissioned Phase 1A privacy/non-interference boundary;
- 2026 calendar/evidence-gate contract;
- stable development rules and canonical ownership pointers;
- constructive-disagreement principle.

It no longer owns:

- the exact startup file sequence;
- detailed package/stage/commit/push actor mechanics;
- Phase 1A candidate chronology superseded by commissioning;
- raw Phase 1A timing metrics;
- the generic staging preflight failure story;
- active next-action/checkpoint chronology.

## Information Preservation

No unique scientific conclusion is intentionally removed.

Detailed validation and historical context remain in canonical records already
present under:

- `decisions/`;
- `evidence/`;
- dated memory/history;
- `patches/PATCH_PROTOCOL.md`;
- `COMMUNICATION.md`;
- `TOOLS.md`;
- `CURRENT.md` and roadmap status for the active frontier.

The commissioned Phase 1A conclusion remains in curated memory, while its
preflight and runtime evidence remain canonical in dedicated evidence files.

## Scope

Rewritten:

- `docs/memory/MEMORY.md`;
- `docs/memory/CURRENT.md`;
- `docs/memory/handoffs/CURRENT_HANDOFF.md`;
- `docs/memory/roadmap/STATUS.md`;
- `docs/KNOWN_ISSUES.md`.

Created:

- this evidence record.

## Explicitly Unchanged

M3B does not modify:

- startup membership/ordering in `AGENTS.md` or `MAINTENANCE.md`;
- `COMMUNICATION.md`, `TOOLS.md`, or checkpoint mechanics;
- health-checker implementation;
- football/model/application source;
- commissioned runtime;
- retained Phase 1B candidate;
- persistent evidence authorization.

## Validation Contract

The M3B package must:

- authorize each existing target against the durable M3A Git-blob predecessor
  while tolerating only BOM/newline representation differences;
- preserve existing target representation;
- apply the exact reviewed replacements;
- support exact idempotent rerun;
- roll back all affected paths on post-write failure;
- prove the curated-memory required sections remain;
- reject removed role-drift markers such as the old Phase 1A candidate section,
  default actor sequence, mandatory-bootstrap ownership, and staging-preflight
  chronology;
- run strict memory health;
- leave `docs/memory/manifest.json` unchanged during local apply;
- leave the control-root Git index unchanged;
- not touch the commissioned runtime.

## Next Memory Step

Once M3B is durable on remote `main`, M3 is complete. Proceed to
**M4 — handoff contract** only.

Week 3 prospective capture remains a harder calendar gate than nonessential M4-M7
progress.
